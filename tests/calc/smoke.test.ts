import { describe, expect, it } from "vitest";

// Placeholder to prove the Vitest pipeline is wired correctly.
// Real calculation-engine tests (Section 10 acceptance values) land in Phase 1.
describe("scaffold", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
