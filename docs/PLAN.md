# HIYYA Command Center — build plan

Status: **Stage A finished (Phases 0–7); Phase 8 (UI/UX audit), Phase 9 (React performance review), Phase 10 (themed throne redesign), Phase 11 (compact layout pass), Phase 12 (hero-band de-duplication), Phase 13 (per-branch trend breakdown), Phase 14 (attention panel repositioned), Phase 15 (branch ranking as a leaderboard), Phase 16 ("Hiyya"-prefixed branch short names), Phase 17 (Monthly profit & margin added to Overview), Phase 18 (branch names on the SOP deviation heatmap/table), Phase 19 (number legibility pass + app rename), Phase 20 (Branches tab rebuilt around the leaderboard, with medals), Phase 21 (Revenue share's second chart), Phase 22 (persona roster cut to four), Phase 23 (multi-select branch filter on the trend chart), Phase 24 (trend table's period column shows the actual month), and Phase 25 (3D throne removed, dark/pastel theme system added) have since landed.** This plan follows the Phase build prompt (pasted into chat on 2026-09-17; the earlier v3 spec is preserved at `reference/hiyya-analytics-platform-prompt-v3.md` for brand/context background, and is superseded wherever the two differ).

- Phase 0 (scaffold) — done, commit `38f7c32`.
- Phase 1 (data layer + calc engine) — done, commit `4dad6f0`. All 41 Vitest cases pass, including the full Section 10 acceptance table and the exact 10 "Investigate" flags.
- Phase 2 (shell + 3D throne stage) — done, commit `c103c92`. 24/24 Playwright cases pass; the demo-date bug found and fixed mid-phase (August only had 30 days of generated sales, so "today" — 31 Aug — showed 0) is documented there.
- Phase 3 (Brand Owner: all six tabs with drill-downs) — done, commit `34006ea`. 53/53 Vitest cases pass; two content bugs (a wrong month label, a wrong month name in a subtitle) and one screenshot-script false alarm are documented there.
- Phase 4 (Brand Manager: shared tabs + SOP recipe editor + impact preview) — done, commit `3572c4a` (plus an access-control consistency fix, commit `071c786`, found during a full-implementation audit). 53/53 Vitest, 32/32 Playwright cases pass.
- Phase 5 (Branch Owner: At a glance, editable fixed-cost grid with live recalculation, scoped Sales/SOP) — done, commit `8cd3bce`. 57/57 Vitest, 46/46 Playwright cases pass.
- Phase 6 (Branch Manager, mobile-first: Today, Purchases, Stock & SOP, Wastage) — done, commit `b9dcb03`. Found and fixed a real mobile bug in the shared toast component (it was pinned to the top of the viewport below the `sm:` breakpoint, covering the header/persona-switcher/tab bar — invisible in every prior phase's desktop-first testing, load-bearing here since Purchases/Wastage rely on the toast as their confirmation). 57/57 Vitest, 60/60 Playwright cases pass.
- Phase 7 (Polish: accessibility, performance, reduced motion, README, deploy) — done, see the Phase 7 report. Lighthouse on Overview: Accessibility 100, Best Practices 100, SEO 91 (all comfortably over the ≥85 target); Performance improved 48 → ~65 via two real architectural fixes (per-role/per-tab code-splitting cut First Load JS ~597 kB → ~193 kB; a font-display fix removed the FOUT-driven layout shift), though it doesn't clear 85 on its own — documented honestly in the report rather than papered over. Fixed a real, if minor, accessibility bug (KPI-card captions were marked as `<h3>` headings; several section headings skipped a level to h3 with no h2 — both concretely flagged by Lighthouse's own audit, not guessed at) and an unlabelled `<select>`. Added a global `prefers-reduced-motion` CSS rule alongside the 3D throne stage's existing JS-level handling. Switched `pnpm e2e`'s web server from `next dev` to a production build after finding that Phase 7's own per-tab code-splitting made on-demand dev-mode compilation genuinely flaky under Playwright's parallel workers — not present in the real, already-deployed production app. 57/57 Vitest, 60/60 Playwright cases pass, twice in a row.
- Phase 8 (expert UI/UX audit + Quick Wins) — a full 13-section UI/UX audit against enterprise dashboard standards (published as an artifact), explicitly reconciled against the black-and-gold, no-light-mode brand mandate rather than defaulting to the light-mode benchmarks it was measured against. Implemented the four P0 Quick Wins: a shared `TabSkeleton` replacing the "blank panel, then pop-in" pattern on every tab that blocks on its first fetch (plus a matching `loading` fallback on the `next/dynamic` tab chunks themselves — a second, code-level version of the same gap, found while screenshotting the fix under a throttled connection); toast variants (success/info/warning, icon + semantic left-border) so a confirmation reads as good/neutral/caution news at a glance, not just one more grey card; hover elevation (lift + gold glow) on the two clickable card patterns (theme cards, branch cards); and a shared `EmptyState` component (icon + message) replacing four independently-written single lines of muted text. 57/57 Vitest, 60/60 Playwright.
- Phase 9 (React/Next.js performance review) — a request framed around a Node.js API + SQL Server + Redis stack this project doesn't have (no `app/api` route, no `mssql`/`redis` dependency anywhere) was redirected to what's actually here — published as an artifact with the stack mismatch stated up front rather than a fabricated SQL Server/Redis analysis. Implemented the one real P0 finding: every chart rebuilt its ECharts `option` object inline on every render, and the shared `Chart` wrapper forced `notMerge` unconditionally, so a chart fully destroyed and rebuilt itself on any unrelated parent re-render (a grain toggle, a rank-metric switch), not just when its own data changed. Wrapped every chart's `option` in `useMemo` (7 dedicated chart components + 2 inline usages in SalesTab/SopTab) and dropped `notMerge` from `components/charts/Chart.tsx`, so ECharts's default diff-merge only touches what changed. Also did the P1 finding correctly rather than cosmetically: `React.memo` on `LeagueTable`/`IngredientVarianceTable` row components, plus `useCallback`-stabilizing the `onRowClick` callbacks passed into them from `BranchesTab`/`SopTab` — memoizing a row while its `onClick` prop is still a fresh inline arrow function every render would silently hold a stale closure instead of actually skipping a re-render, so both halves shipped together. Verified with a Playwright script that repeatedly toggles Overview's grain and reopens the SOP heatmap/drill-down after the `notMerge` removal, confirming no stale-series rendering artifacts. 57/57 Vitest, 60/60 Playwright, First Load JS unchanged at 193 kB.
- Phase 10 (themed throne redesign) — the client shared 15 photos of two real physical HIYYA throne installations (a bronze/gold phoenix throne for Dino Mandi, a bone/antler throne for Chrono Jail Mandi) and asked to replace the app's single procedurally-generated 3D throne with these designs. Since the existing throne is code-built three.js geometry, not a 3D scan or photo, a pixel-faithful recreation of ornate physical sculpture wasn't achievable through code — offered three tradeoffs and the client chose a stylized procedural redesign: keep the throne code-generated, but redesign the shared geometry per branch theme to evoke each reference rather than replicate it literally. Built a "throne skin" system: `lib/theme/throneSkins.ts` maps each branch's existing `theme` field (`Dino` / `Chrono Jail` / `Jail` / `Space`) to a skin, reusing the original shared structure (dais, legs, seat, back panel, armrests, pillars) across all five skins (the original gold "signature" skin plus four new ones) while swapping in per-skin materials (bronze/patina-gold/phoenix-plumage/ember-glow for Dino; bone/dark-stone for Chrono Jail; iron/dark-iron for Jail; platinum/star-glow for Space) and a per-skin decorative "topper" (a phoenix with fanned wings for Dino, six branching antler/bone spikes for Chrono Jail, a padlock-and-shackle for Jail, a tilted halo ring with glowing stars for Space, replacing the original five-spike crown only on the signature skin). The hero throne and orbit ring both key off the viewer's persona/scope, so a Dino Mandi branch manager sees the phoenix throne while the brand-wide view keeps the original signature throne rotating among the others. Verified structurally sound (no z-fighting or exploded geometry) via boosted-brightness diagnostic renders of the two most detailed new skins (Dino, Chrono Jail) — diagnostic screenshots were not kept, since they don't represent real production rendering. 57/57 Vitest, 60/60 Playwright.
- Phase 10 follow-up (single throne control) — replaced the two-state "Pause motion"/"Play motion" toggle with a single button cycling three states: play (rotating) -> pause (frozen, still visible) -> hidden (throne stage swapped for the existing static gold-gradient fallback, the same one already used when WebGL is unavailable). Renamed the store's `motionEnabled` boolean to a `throneMode: "play" | "pause" | "hidden"` enum (still the one value that persists across a reload, per the Section 4/14 guardrail). 57/57 Vitest, 60/60 Playwright.
- Phase 11 (compact layout pass) — the client flagged that the KPI row (and the page generally) left a lot of blank space, and asked for the whole UI to read at a single glance at 80% browser zoom. Screenshotting every persona's tabs at 1440x900 with a simulated 80% zoom confirmed the single biggest offender: `HeroBand` reserved a 230px-tall band (mostly empty, behind the 3D throne) above content on every tab. Cut it to 140px (220px on the sub-760px stacked layout), tightened its own padding/type scale, and shaved padding/margins across the board: the header, tab bar, and breadcrumb strip; every KPI/leak card (`p-4`→`p-3`, tighter internal spacing); every panel/chart-frame wrapper (the one shared `rounded-xl border ... bg-hiyya-panel-2/30 p-4` class used by 14 components, `p-4`→`p-3`); and each tab's outer section gap (`gap-6`→`gap-4`) and KPI-row grid gap (`gap-4`→`gap-3`). Net effect: Overview's headline, four KPI cards, trend chart, branch ranking, and attention list — previously requiring a scroll to see in full even at 80% zoom — now render in one 1440x900 view. 57/57 Vitest, 60/60 Playwright (including the 390px no-horizontal-overflow checks, confirming the tighter spacing didn't regress mobile).
- Phase 12 (hero-band de-duplication) — a follow-up: the client pointed out that for Brand Owner/Manager and Branch Owner, the hero headline ("Your 4 branches made ₹1.34 Cr in August") and its subtitle ("+9.2% vs July · net profit ₹18.2 L · ₹1.9 L lost beyond SOP") just restated numbers already on the KPI cards below. Two things stood in the way of simply deleting it: it's the page's only `<h1>` (removing it entirely would undo the Phase 7 heading-hierarchy fix), and for Branch Managers the same headline shows something the cards don't ("Dino Mandi is at 103% of today's target" — no card repeats that). Resolved by keeping the headline as a real `<h1>` everywhere (still one top-level heading per page) but shrinking it to a small, single-line, unobtrusive label and dropping the redundant subtitle entirely; `HeroBand` is now a 44px strip instead of 140px. The one genuinely new number the subtitle carried — growth vs July, previously a small delta chip tucked inside the Net Sales card — was promoted to its own fifth KPI card (`lg:grid-cols-4` → `lg:grid-cols-5`) on Overview and At a Glance, the two tabs that had it. 57/57 Vitest, 60/60 Playwright.
- Phase 13 (per-branch trend breakdown) — the client asked for the Overview trend chart's hover tooltip to break sales down by branch, plus a toggle to view each branch as its own line instead of the brand-wide aggregate. Added: an enhanced combined-view tooltip that lists the aggregate Sales/Expenses/Profit plus a per-branch sales/expenses breakdown underneath; and a "Combined" / "By branch" toggle (shown only when scope is "all branches" — it's meaningless once scope narrows to one) that swaps the three aggregate lines for one sales (solid) + expenses (dashed) pair per branch, each in that branch's own color from the existing `branchColors` token. Caught and fixed two real bugs surfaced while verifying by hovering/toggling repeatedly rather than just once: (1) a stale-tooltip bug — the branch view's tooltip config only set `valueFormatter`, not `formatter`, so ECharts' deep-merge (Phase 9's `notMerge`-less `Chart` wrapper) left the combined view's custom `formatter` function merged in underneath, rendering both; fixed by giving both views an explicit `formatter`. (2) a stale-series bug — toggling from 8 branch series back down to 3 combined series left the extra series behind, since index-based diff-merge doesn't clear array entries beyond the new length; `Chart` gained an opt-in `notMerge` prop (default unset, so every other chart's Phase 9 perf behavior is untouched) and `TrendChart` — the one chart whose series shape actually changes at runtime — opts into it. Verified with a Playwright script that toggles the view and hovers repeatedly in a loop; both bugs were confirmed present, fixed, then confirmed gone. 57/57 Vitest, 60/60 Playwright.
- Phase 14 (attention panel repositioned) — client asked for Overview's "Needs your attention" panel to move above the trend chart, and its flags to lay out across the screen in 2–4 columns instead of one long vertical list. Moved it from a paired `lg:grid-cols-2` slot next to Branch ranking (which now stands full-width on its own) to right after the KPI row, and changed its list from `flex flex-col` to a `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` grid — the same responsive-columns pattern already used by the KPI row. Checked at 1440px, 700px, and 390px to confirm the column count steps down cleanly with no overflow. 57/57 Vitest, 60/60 Playwright.
- Phase 15 (branch ranking as a leaderboard) — client felt the "Branch ranking" bar chart didn't read as a ranking, and asked for it to behave like a leaderboard that visibly reshuffles when the ranked metric changes, with the #1 branch getting a star, positioned beside "Needs your attention" rather than as its own full-width chart. Replaced the `RankChart` bar chart (still used as-is on the Branches tab — untouched) with a new `components/kpi/Leaderboard.tsx`: numbered rows already sorted best-first by `ds.getRank` (which already handles lower-is-better metrics like SOP deviation % via `rankBy`'s `lowerIsBetter` flag), rank 1 gets a gold-tinted row and a star icon, each row shows its branch's existing color dot and is a real clickable button (opens the same branch quick-view drilldown as before). Re-paired it with "Needs your attention" in a `lg:grid-cols-2` row (capping the attention grid at 2 columns instead of Phase 14's 4, since it's now half-width) directly above the trend chart. The list is keyed by the selected metric and uses `tailwindcss-animate`'s `fade-in`/`slide-in-from-top` utilities with a small per-row stagger to cue "the leaderboard just reshuffled," deliberately not reaching for Framer Motion (per this project's convention, reserved for panel/drawer transitions). Verified by switching metrics and confirming rows genuinely reorder (e.g. Dino jumps to #1 on SOP deviation, Chrono Jail leads on net sales/profit), the drilldown click-through still works, and there's no horizontal overflow at 390px. 57/57 Vitest, 60/60 Playwright.
- Phase 16 ("Hiyya"-prefixed branch short names) — client asked for the leaderboard's branch labels ("Chrono Jail", "Dino", "Jail Vizag", "Space") to read "Hiyya Chrono Jail", "Hiyya Dino", "Hiyya Jail Vizag", "Hiyya Space"; confirmed via a clarifying question that this should apply everywhere the app shortens a branch's full name (e.g. "Chrono Jail Mandi") to fit a chart legend, axis label, or card title — not just the new leaderboard. That shortening logic turned out to be duplicated inline (`branchName.replace(" Mandi", "")`) across seven call sites in five tab components; consolidated it into one `shortBranchName()` helper in `lib/calc/format.ts` and updated every call site (Overview's leaderboard/trend-legend/theme-cards, Sales's stacked-bar legend, Branches' rank chart, Expenses' stacked-bar categories, Revenue share's donut legend) to use it. Left full-name displays (LeagueTable's "Branch" column, RevenueShareTable) untouched, since those show the complete brand name, not a shortened label. Verified visually across Overview, Sales, Branches, and Revenue share. 57/57 Vitest, 60/60 Playwright.
- Phase 17 (Monthly profit & margin added to Overview) — client liked the Expenses tab's "Monthly profit & margin" chart (net profit by month, year-to-date, unaffected by the daily/weekly/monthly grain toggle elsewhere) and asked for it on Overview too. Added a second, independent `ds.getPnlSeries(scope, "monthly", PERIOD)` fetch on Overview (the existing one respects the grain toggle and is trimmed to the first trading period, which this chart deliberately isn't) and a new `ChartFrame` right after the main trend chart, reusing the same `TrendChart` component and matching title/subtitle/accessible-table exactly as they appear on Expenses. 57/57 Vitest, 60/60 Playwright.
- Phase 18 (branch names on the SOP deviation heatmap/table) — client asked for branch names, not raw codes (B01–B04), on the SOP tab's "Deviation heatmap" y-axis and the "Ingredient variance" table's Branch column. Added a `branchDisplayName()` helper local to `SopTab.tsx` (and a matching one in `IngredientVarianceTable.tsx`, which only receives `branchCode` on its row type and has no reason to import a whole tab's state) that resolves a branch code to its `shortBranchName()` label, and used it for: the heatmap's y-axis labels (a new `branchNames` array kept in the same order as the code-indexed `branches` array so `heatmapValues`' index math is untouched), its accessible-table alternative, the "Biggest leaks, by value" list and its accessible table, and `IngredientVarianceTable`'s Branch column. 57/57 Vitest, 60/60 Playwright (one Branch Manager wastage-flow test flaked on the first run — confirmed unrelated and non-flaky by re-running in isolation, then a full clean re-run).
- Phase 19 (number legibility pass + app rename) — client said numbers across the app weren't clearly visible, and small text was hard to read at a glance, pointing to the "Performance by theme" cards' Net sales/Margin/SOP deviation loss values as an example; also asked for the app name to become "HIYYA Kitchens Command Center". Root-caused the number-legibility complaint to two patterns, both fixed: (1) `KpiCard` and `LeakCard` — the two components behind nearly every KPI number in the app — rendered their value text as a `bg-clip-text text-transparent` gradient (`champagne → gold → deep-gold` for KpiCard); the darkest stop had weak contrast against the dark panel background, especially at the `text-xl` size Phase 11 already shrank these to. Replaced both with a solid, bright, guaranteed-legible color (`text-hiyya-champagne` / `text-hiyya-warning`) — no more risk of a number landing in a low-contrast part of a gradient. (2) The "Performance by theme" cards (Overview) and "Your branches" cards (At a glance) rendered their Net sales/Margin `<dd>` values with no color or weight at all, same visual weight as plain body text — bumped them to `font-bold text-hiyya-text` at a larger `text-sm` list size, matching the SOP-deviation-loss line that was already bold. Renamed the app in the header, the browser-tab `<title>`, and the one smoke test that asserted the old text. 57/57 Vitest, 60/60 Playwright.
- Phase 20 (Branches tab rebuilt around the leaderboard, with medals) — client wanted the Branches tab enhanced significantly: a proper KPI row (it was the one tab in the app with none — every other tab leads with four cards), the ranking chart replaced with the same leaderboard used on Overview, moved to the top, and gold/silver/bronze medal styling. Added four KPI cards (Net sales, Top performer, Average margin, Lost to SOP deviation — the last reusing the same `ds.getIngredientVariance` sum-across-branches pattern as Overview/Glance) ahead of the leaderboard. Extended the shared `Leaderboard` component itself (used by both Overview and Branches) with gold/silver/bronze badge and row-tint styling for ranks 1–3, built from existing tokens (`hiyya-gold`/`hiyya-platinum`/`hiyya-bronze` — no new colors introduced) rather than a one-off local implementation, so Overview's leaderboard picked up medals too. Replaced the old `RankChart` bar chart with `Leaderboard`, and deleted `RankChart.tsx` once confirmed it had no remaining callers anywhere in the app. The League table underneath is untouched. Verified the drilldown click-through still opens the right branch's quick view, and no horizontal overflow at 390px. 57/57 Vitest, 60/60 Playwright.
- Phase 21 (Revenue share's second chart) — client asked for a second chart to the right of Revenue share's donut, picking "a good KPI" and displaying it in an enlightening way. Paired the existing "Revenue share" donut (share of net sales) with a new "Contribution to brand revenue" stacked bar chart (royalty + marketing fund each branch actually pays, from the same `RevenueShareRow` data already being fetched — no new `DataSource` method needed), reusing the existing `StackedBarChart` component. The pairing itself is the insight: Chrono Jail Mandi has the largest net-sales share (30%) but the **shortest** bar on the right — actually zero, since it's the brand-owned flagship and pays neither royalty nor marketing fund — a contrast the donut alone can't show. 57/57 Vitest, 60/60 Playwright.
- Phase 22 (persona roster cut to four) — client asked for the "View as" list to hold exactly four personas: Brand Owner Gautamy Chowdary (unchanged), Brand Manager Gowtham Muvva (renamed from Ravi Teja), Branch Owner Yugander (renamed from Suresh Reddy, kept on the multi-branch B02+B03 scope rather than Anil Kumar's single-branch B04, to keep the portfolio-view/anonymised-rank code paths under test), and Branch Manager Satish (renamed from Kiran, kept on B02 rather than Farhan's B01). Removed Anil Kumar and Farhan as personas entirely, updated `PersonaId`'s Zod enum from six IDs to four, and updated the two branches' (B02/B03) `ownerName` from "Suresh Reddy" to "Yugander" to match the renamed owner (B04's `ownerName` stays "Anil Kumar" — that franchise owner isn't simulated as a "View as" persona anymore, but the name is still real branch metadata). Applied the same edits to `scripts/build-demo-data.ts`, the generator `lib/data/demo.json` was originally produced from, so regenerating the dataset wouldn't silently reintroduce the old six-persona roster. Updated the two e2e specs that exercised the removed personas — renamed `Suresh Reddy`→`Yugander` and `Kiran`→`Satish` throughout `branch-owner.spec.ts`/`shell.spec.ts`/`branch-manager.spec.ts`, and deleted the now-impossible "Anil Kumar (single branch)" test — plus the README's persona table and intro line ("six personas" → "four personas"). 57/57 Vitest, 58/58 Playwright (2 fewer than before, for the exact 2 tests removed).
- Phase 23 (multi-select branch filter on the trend chart) — client asked for the "By branch" view of the Sales/expenses/profit trend chart to let the user multi-select which branches show, for a more interactive chart. Added a row of toggle chips (branch color dot + name, `aria-pressed`) above the chart, one per branch, shown only in "By branch" view; toggling one filters a new `visibleBranchSeries` (derived from the existing `branchSeries` state via a `hiddenBranches: Set<string>`) that feeds both the chart and its accessible-table alternative, so the two never drift apart. Guarded against selecting down to zero branches — toggling off the last visible one is a no-op, since an empty chart isn't useful — verified by deliberately trying to hit zero in a Playwright check. The "Combined" view's hover tooltip (Phase 13's per-branch breakdown) deliberately keeps reading the *full*, unfiltered `branchSeries` rather than the chip selection, since the chips themselves are invisible in that view and silently filtering a tooltip nobody can see the reason for would be confusing. 57/57 Vitest, 58/58 Playwright.
- Phase 24 (trend table's period column shows the actual month) — client pointed at the "View as table" alternative for the trend chart's daily "By branch" view, where the first column was just labelled "Period" over bare day-of-month numbers ("01".."31") with no month/year context. Added a `periodColumnLabel` computed from the tab's fixed `PERIOD` constant ("2026-08" → "Aug-26") for the daily/weekly grains, where every row really is a day or week within that one month; left it as "Month" for the monthly grain, where each row is already a distinct month (Dec through Aug) and a single "Aug-26" header would be wrong. Applied to both of the trend chart's accessible-table variants (combined and by-branch). 57/57 Vitest, 58/58 Playwright.
- Phase 25 (3D throne removed, dark/pastel theme system added) — client asked for the rotating-chair (3D throne) background to be removed completely, and for a theme option with a good pastel palette on a white/subtle background, with different colors and lighting for the whole screen. This reversed two of the project's original firm conventions (the Section 8/9 procedural 3D throne stage, and CLAUDE.md's explicit "no light mode") — both updated to match. Deleted `components/three/*` (7 files), `lib/theme/throneSkins.ts`, and `components/shell/MotionToggle.tsx` wholesale, and removed `three`/`@react-three/fiber`/`@react-three/drei`/`@types/three` from `package.json` via `pnpm remove` (not just deleted from the file — the lockfile needed updating too). `useAppStore`'s `throneMode`/`cycleThroneMode`/`THRONE_MODE_ORDER` became a `theme: "dark" | "pastel"` field and `toggleTheme()` action, still the one preference that persists across a reload. Replaced the throne's fixed 3D canvas with `components/shell/BackgroundLayer.tsx` — a static, full-viewport CSS gradient, a different subtle multi-tone "lighting" treatment per theme (a warm gold glow on black for dark; a warm amber-and-terracotta double glow on cream for pastel), with `AppFrame.tsx` syncing `<html data-theme>` to the store. Designed a specific warm pastel palette (`lib/theme/tokens.ts`) rather than a generic one — cream background, white cards, four dusty warm accents (amber/peach/terracotta/taupe) standing in for the dark theme's gold/champagne/bronze/platinum branch colors, and soft sage/coral (not saturated green/red) for gain/loss — deliberately kept in the same warm family as the original gold theme rather than a cool blue-ish "AI pastel," so switching themes still reads as HIYYA. The hard part: `hiyyaColors`/`branchColors`/`goldRamp` were previously static, dark-only constants imported directly into 14 chart/tab files (and `lib/theme/echarts-theme.ts`, which registers ECharts' own theme object) — every one of those had to switch to a `useThemeColors()` hook read at render/option-build time (added to `useMemo` dependency arrays where the color was baked into a chart's `option`, and — the one non-obvious case — added to a `useEffect`'s dependency array in `OverviewTab.tsx` where a branch's color was being baked into fetched-and-stored `branchSeries` state, which wouldn't otherwise have re-derived on a theme switch). Deleted the static exports entirely rather than keeping a backward-compatible dark default, so TypeScript itself would catch any missed call site — it caught none, confirming the sweep was complete. `ReactECharts` now receives `theme={echartsThemeName(theme)}`, resolving to one of two themes (`hiyya-dark`/`hiyya-pastel`) registered from the same palette. Removed the now-meaningless "play/pause/hide the throne" e2e test and replaced it with one exercising the theme toggle (switches, persists across reload, switches back). 57/57 Vitest, 58/58 Playwright (1-for-1 test replacement, same count as Phase 24). Visually verified every tab, in both themes, across all four personas.

## 0. What exists today, and what doesn't carry over

The repo currently contains one artifact from an earlier, separate engagement: `hiyya-brand-owner-dashboard.html`, a self-contained static HTML/CSS/vanilla-JS demo with fabricated numbers and a persona switcher, deployed to GitHub Pages. It is **not** the `reference/` input this prompt expects (see `docs/QUERY_TRACKER.md` Q00a) — its dataset format and its actual numbers both predate and conflict with this prompt's Section 10 acceptance table. It stays in the repo, untouched, as a historical artifact and a rough IA reference, but Stage A is a fresh Next.js codebase, not a refactor of that file.

## 1. Sequencing

Phases run in the exact order in Section 12 of the build prompt. Each phase ends with lint + typecheck + test green, a commit (`phase-N: <summary>`), and a stop-and-report using the Section 13 format. No phase starts without an explicit "go" on the previous one's report.

| Phase | Scope                                                                                                                                                                                                               | Key files/dirs created                                                                                       |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| 0     | Scaffold: Next.js 14 (App Router) + TS strict, Tailwind, shadcn/ui primitives, ESLint/Prettier, Vitest, Playwright, pnpm scripts. Design tokens as CSS variables. Fonts (Cormorant Garamond, Manrope).              | `app/layout.tsx`, `app/page.tsx`, `lib/theme/tokens.ts`, `CLAUDE.md`, `package.json`, config files           |
| 1     | Data layer + calc engine. Extract/seed scripts, Zod types, `DataSource` interface, `MockDataSource`, all of `lib/calc/*`, Section 10 acceptance tests passing in Vitest.                                            | `lib/data/*`, `lib/calc/*`, `scripts/*`, `tests/calc/*`, `docs/DATA_CONTRACT.md`                             |
| 2     | Shell: header, persona switcher, scope select, tabs, breadcrumb, hero band, toast, Reset-demo, ECharts theme registration, 3D throne stage (all persona behaviours, motion toggle, WebGL/reduced-motion fallbacks). | `components/shell/*`, `components/three/*`, `lib/store/*`, `lib/theme/echarts-theme.ts`                      |
| 3     | Brand Owner: all six tabs with drill-downs.                                                                                                                                                                         | `app/[tab]/page.tsx` routes, `components/kpi/*`, `components/charts/*`, `components/tables/*` (brand-scoped) |
| 4     | Brand Manager: shared tabs + SOP recipe editor + impact preview.                                                                                                                                                    | `components/forms/SopRecipeEditor.tsx`                                                                       |
| 5     | Branch Owner: At a glance, editable fixed-cost grid with live recalculation, scoped Sales/SOP.                                                                                                                      | `components/forms/FixedCostGrid.tsx`                                                                         |
| 6     | Branch Manager (mobile-first): Today, Purchases, Stock & SOP, Wastage.                                                                                                                                              | `components/forms/PurchaseForm.tsx`, `WastageForm.tsx`, mobile layout variants                               |
| 7     | Polish: accessibility audit, contrast, reduced motion, Lighthouse ≥85 on Overview, full Playwright suite green, README with screenshots, Vercel deploy.                                                             | `docs/screenshots/phase-7/*`, README updates                                                                 |

Stage B (Phases 8–12: Supabase schema/RLS, auth, importers, alerts/exports/audit, later modules) does not start until the client confirms the work order, per the prompt's Section 4 and 12.

## 2. Data fidelity strategy (given Q00/Q00a are open)

Phase 1 cannot wait indefinitely on the source workbook, so the demo dataset will be built **top-down from the Section 10 acceptance table**, which is authoritative and exact:

- The four branches' monthly net sales, food-cost-at-SOP, actual food cost, and net profit are fixed to the published figures (±₹50 tolerance is the test tolerance, not license to round differently).
- The 10 named "Investigate" ingredient lines, and the brand-wide unexplained-deviation (₹1,79,962) and logged-wastage (₹92,216) totals, are fixed the same way.
- Everything the workbook would normally supply but the acceptance table doesn't specify — ingredient-level SOP quantities and rates, daily sales curves, item mix, channel splits — will be **derived, not invented freely**: built with a documented, seeded allocation method so it reconciles exactly to the numbers above, and flagged in `docs/DATA_CONTRACT.md` as "demo-derived, pending workbook" wherever it's below the acceptance table's level of detail. This keeps the letter of "never hardcode a figure in the UI" (every screen number still traces to `lib/calc`, not a magic constant) while being honest that the **inputs** to that engine are a documented demo allocation until Q00 is answered.

## 3. Open questions (blocking or not)

See `docs/QUERY_TRACKER.md` for the full list (Q00, Q00a, and the seeded Q01–Q13 from the v3 spec). None of these block Phase 0 (pure scaffolding). Q00/Q00a block Phase 1 from being more than "self-consistent with Section 10" rather than "verified against the client's real workbook" — flagging now so it isn't a surprise at Phase-1 sign-off.

Additional process questions:

- **Package manager**: prompt specifies pnpm — confirming it's fine to install pnpm globally in this environment (not yet verified as available).
- **Deploy target**: an existing GitHub repo (`haneelteja/Hiyya-Kitchens-Demo`) and Vercel account access were used for the earlier static-HTML demo. Reuse the same GitHub repo for the Next.js app (as a new branch or by replacing main), or start a fresh repo? Recommend a fresh repo given this is architecturally a new project, but the choice is yours.
- **Design tokens**: the earlier HTML demo's palette is close to but not byte-identical to this prompt's Section 9 table (e.g. `bg` was `#050402` there vs. `#000000` here). Phase 0 will use this prompt's exact values.

## 4. Definition of done per phase

Lint clean, `tsc --noEmit` clean, Vitest green (Phase 1 onward includes the Section 10 acceptance suite), Playwright green (from Phase 2's shell tests onward), a real commit, and a phase report in the Section 13 format with screenshots at 1440px and 390px under `docs/screenshots/phase-N/`.

---

**Waiting for "go" to start Phase 0.**
